name: Build
on: [push, pull_request]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            dir: ./out/make/deb/x64/
          - os: windows-latest
            dir: ./out/make/squirrel.windows/x64/
          - os: macos-latest
            dir: ./out/make/zip/darwin/

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@master
      with:
        node-version: 14
    - name: install dependencies
      run: npm install
    - name: build
      run: npm run make
    - name: list files
      run: ls ${{ matrix.dir }}
